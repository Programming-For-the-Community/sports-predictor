# boto3 is pre-installed in the Lambda Python runtime -- not bundled here.
# The shared library (Source/library) is installed separately by the build
# workflow.
#
# Same package set and reasoning as
# Source/aws-lambdas/ncaafb/predict/requirements.txt -- this Lambda's
# model_loader.py is a byte-for-byte copy of NCAAFB's/NFL's own,
# dispatching on library.ml.model_types.ADAPTERS the same way, so it needs
# the same deserialization footprint regardless of which algorithm
# actually won any given NBA model's own backtest tournament.
#
# numpy/scipy: xgboost's own real runtime deps (installs --no-deps and
# won't pull them in on its own).
# scikit-learn/joblib/pandas: needed whenever a promoted model is a
# non-XGBoost candidate (LogisticRegression/RandomForest/MLP -- see
# library/ml/model_types.py's ADAPTERS).
# lightgbm: NBA's own CANDIDATES lists include LightGBM (unlike NFL's/
# NCAAFB's, which don't use it yet) -- unconditional here for the same
# reason scikit-learn is, since which algorithm actually won any given
# target's tournament can change from one retrain to the next. Needs
# libgomp at runtime too -- see this directory's own Dockerfile.
numpy
scipy
scikit-learn
joblib
pandas
lightgbm>=4.3
