# boto3 is pre-installed in the Lambda Python runtime -- not bundled here.
# The shared library (Source/library) is installed separately by the build
# workflow.
#
# Same package set and reasoning as
# Source/aws-lambdas/nfl/predict/requirements.txt -- this Lambda's
# model_loader.py is a byte-for-byte copy of NFL's own, dispatching on
# library.ml.model_types.ADAPTERS the same way, so it needs the same
# deserialization footprint regardless of which algorithm actually won
# any given NCAAFB model's own backtest tournament.
#
# numpy/scipy: xgboost's own real runtime deps (installs --no-deps and
# won't pull them in on its own).
# scikit-learn/joblib/pandas: needed whenever a promoted model is a
# non-XGBoost candidate (ElasticNet/RandomForest/MLP -- see
# library/ml/model_types.py's ADAPTERS).
numpy
scipy
scikit-learn
joblib
pandas
