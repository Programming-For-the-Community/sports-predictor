# Same dependency set as model-training/nfl -- see that requirements.txt's
# own comment -- plus lightgbm, which NFL's/NCAAFB's own training images
# don't need (their CANDIDATES lists don't include the LightGBM adapters
# -- see library/ml/model_types.py's own docstring on why the import
# stays lazy/adapter-scoped rather than a library-wide dependency).
# lightgbm ships prebuilt manylinux wheels for standard Fargate
# architectures, same as xgboost.
pandas>=2.2
pyarrow>=16.0
scikit-learn>=1.4
scikit-learn-intelex>=2024.0
# Pinned to the EXACT version aws-lambdas/nba/predict/requirements-nodeps.txt
# also pins -- xgboost's raw model format is not guaranteed compatible
# across major versions; letting each side resolve `>=2.0` independently
# let a real prod bug through, where an older predict-side xgboost
# silently mispredicted a model this image had trained under a newer one.
xgboost==3.3.0
lightgbm>=4.3
