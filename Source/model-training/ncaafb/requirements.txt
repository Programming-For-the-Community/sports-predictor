# Same dependency set as model-training/nfl -- see that requirements.txt's
# own comment. Shared by both Dockerfiles in this directory (Dockerfile and
# Dockerfile.ranking) -- every NCAAFB training script needs the same
# xgboost/pandas/pyarrow/scikit-learn/scikit-learn-intelex stack.
pandas>=2.2
pyarrow>=16.0
scikit-learn>=1.4
scikit-learn-intelex>=2024.0
# Pinned to the EXACT version
# aws-lambdas/ncaafb/predict/requirements-nodeps.txt also pins -- xgboost's
# raw model format is not guaranteed compatible across major versions;
# letting each side resolve `>=2.0` independently let a real prod bug
# through, where an older predict-side xgboost silently mispredicted a
# model this image had trained under a newer one.
xgboost==3.3.0
