# Same dependency set as model-training/nba -- see that requirements.txt's
# own comment. lightgbm ships prebuilt manylinux wheels for standard
# Fargate architectures, same as xgboost.
pandas>=2.2
pyarrow>=16.0
scikit-learn>=1.4
scikit-learn-intelex>=2024.0
# Pinned to the EXACT version aws-lambdas/ncaambb/predict/requirements-nodeps.txt
# will also pin, once that Lambda exists -- xgboost's raw model format is
# not guaranteed compatible across major versions; letting each side
# resolve `>=2.0` independently let a real prod bug through for other
# sports (see project-xgboost-version-skew-bug memory), where an older
# predict-side xgboost silently mispredicted a model this image had
# trained under a newer one.
xgboost==3.3.0
lightgbm>=4.3
