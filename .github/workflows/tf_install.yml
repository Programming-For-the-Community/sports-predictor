# This workflow installs the latest version of Terraform CLI and configures the Terraform CLI configuration file
# with an API token for Terraform Cloud (app.terraform.io). On push/pull_requests to tf/dev runs a tf plan

name: AWS Setup

on:
  push: 
    branches:
        - tf/*
  pull_request:
    branches:
        - tf/*
  workflow_dispatch:
    inputs:
      environment:
        description: 'Deployment environment'
        required: true
        default: 'development'
        type: choice
        options:
          - development
          - production
    
  
  workflow_call:
    inputs:
      skip_approval_gate:
        description: 'Set true by a calling workflow that already gates its own approval (e.g. nfl_deploy.yml), so this job does not double-prompt for environment protection.'
        required: false
        type: boolean
        default: false
      server_image_tag:
        description: 'Image tag of the server Docker image'
        required: false
        type: string
      server_version:
        description: 'Version of the server'
        required: false
        type: string
      client_image_tag:
        description: 'Image tag of the client Docker image'
        required: false
        type: string
      client_version:
        required: false
        type: string
        description: 'Version of the client'
    outputs:
      nfl_ingest_function_name:
        description: 'NFL ingest Lambda function name'
        value: ${{ jobs.terraform.outputs.nfl_ingest_function_name }}
      nfl_normalize_function_name:
        description: 'NFL normalize Lambda function name'
        value: ${{ jobs.terraform.outputs.nfl_normalize_function_name }}

permissions:
  id-token: write
  contents: read

env:
  TF_VAR_vpc_id: ${{ secrets.VPC_ID }}
  TF_VAR_vpc_cidr: ${{ secrets.VPC_CIDR }}
  TF_VAR_domain_name: ${{ secrets.DOMAIN_NAME }}
  TF_VAR_hosted_zone_id: ${{ secrets.DOMAIN_HOSTED_ZONE_ID }}
  TF_VAR_account_id: ${{ secrets.AWS_ACCOUNT_ID }}
  TF_VAR_region: ${{ vars.AWS_RESOURCE_REGION }}
  TF_VAR_project: ${{ vars.PROJECT}}
  TF_VAR_owner: ${{ vars.OWNER}}
  TF_VAR_alert_email: ${{ secrets.ALERT_EMAIL }}
  TF_VAR_environment: ${{ github.event.inputs.environment || 'development' }}
  TF_VAR_private1_subnet_cidr: ${{ vars.SUBNET_A_CIDR }}
  TF_VAR_private2_subnet_cidr: ${{ vars.SUBNET_B_CIDR }}
  TF_VAR_private3_subnet_cidr: ${{ vars.SUBNET_C_CIDR }}
  TF_VAR_public1_subnet_cidr: ${{ vars.PUBLIC1_SUBNET_CIDR }}
  TF_VAR_public2_subnet_cidr: ${{ vars.PUBLIC2_SUBNET_CIDR }}
  TF_VAR_public3_subnet_cidr: ${{ vars.PUBLIC3_SUBNET_CIDR }}
  TF_VAR_ecr_repo_url: ${{ secrets.ECR_URI }}
  TF_VAR_espn_api_root_url: ${{ vars.ESPN_API_ROOT_URL }}

jobs:
  terraform:
    name: 'Terraform'
    runs-on: ubuntu-latest
    outputs:
      nfl_ingest_function_name: ${{ steps.tf_outputs.outputs.nfl_ingest_function_name }}
      nfl_normalize_function_name: ${{ steps.tf_outputs.outputs.nfl_normalize_function_name }}
    # Environment protection rules are enforced per job, not per workflow
    # run -- so calling this job from another workflow that already has
    # its own approval gate (e.g. nfl_deploy.yml) would otherwise trigger
    # a second, redundant approval prompt here. An empty string means "no
    # environment," which skips gating entirely.
    #
    # This used to key off `github.event_name != 'workflow_call'`, on the
    # assumption that event_name would read as "workflow_call" when this
    # workflow is invoked via `uses:`. It doesn't -- inside a called
    # workflow, github.event_name still reflects whatever triggered the
    # top-level run (e.g. "push", since nfl_deploy.yml triggers on push),
    # so that condition was always true and gating never actually turned
    # off, causing a second approval prompt. skip_approval_gate is an
    # explicit input the caller sets instead of an inferred signal.
    environment: ${{ !inputs.skip_approval_gate && (github.event.inputs.environment || 'development') || '' }}

    # Use the Bash shell regardless whether the GitHub Actions runner is ubuntu-latest, macos-latest, or windows-latest
    defaults:
      run:
        shell: bash

    steps:
    # Checkout the repository to the GitHub Actions runner
    - name: Checkout
      uses: actions/checkout@v4

    # Fails loudly here rather than letting an unset ESPN_API_ROOT_URL
    # repo variable silently resolve to an empty string and propagate all
    # the way into a running container's environment, where it would
    # break every ESPN call without any clear error at the source.
    - name: Validate required variables
      run: |
        if [ -z "$TF_VAR_espn_api_root_url" ]; then
          echo "::error::ESPN_API_ROOT_URL is not set. Create it under Settings > Secrets and variables > Actions > Variables (value: https://site.api.espn.com/apis/site/v2/sports)."
          exit 1
        fi

    - name: Echo vars
      run: |
        echo "client_image: $TF_VAR_client_image"

    - name: Configure AWS Credentials (via OIDC)
      uses: aws-actions/configure-aws-credentials@v4
      with:
        role-to-assume: ${{ secrets.AWS_DEPLOY_SVC_ACCT_ARN }}
        aws-region: ${{ vars.AWS_RESOURCE_REGION }}

    # Install the latest version of Terraform CLI and configure the Terraform CLI configuration file with a Terraform Cloud user API token
    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        cli_config_credentials_token: ${{ secrets.TF_API_TOKEN }}

    # Initialize a new or existing Terraform working directory by creating initial files, loading any remote state, downloading modules, etc.
    - name: Terraform Init
      run: terraform -chdir=Terraform init -reconfigure

    # Checks that all Terraform configuration files adhere to a canonical format
    - name: Terraform Format
      run: terraform -chdir=Terraform fmt -check

    # Validate Terraform
    - name: Terraform Validate
      run: terraform -chdir=Terraform validate

    # Generates an execution plan for Terraform
    - name: Terraform Plan
      run: terraform -chdir=Terraform plan

    # Build or change infrastructure according to Terraform configuration files.
    - name: Terraform Apply
      run: terraform -chdir=Terraform apply -auto-approve

    - name: Capture Terraform outputs
      id: tf_outputs
      run: |
        echo "nfl_ingest_function_name=$(terraform -chdir=Terraform output -raw nfl_ingest_function_name)" >> "$GITHUB_OUTPUT"
        echo "nfl_normalize_function_name=$(terraform -chdir=Terraform output -raw nfl_normalize_function_name)" >> "$GITHUB_OUTPUT"